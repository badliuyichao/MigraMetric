# 异构系统升迁工作量评估系统 - 需求说明文档

## 一、项目概述

### 1.1 项目背景
企业长期使用ERP系统，现采购新的ERP系统，需要将旧系统的历史数据迁移到新系统。本系统旨在建立一个Web工具，用于评估异构ERP系统数据迁移的工作量，为客户提供量化、可视化的评估报告。

### 1.2 项目目标
- 建立科学的工作量评估模型
- 提供便捷的Web工具进行工作量评估
- 输出详细的工作量评估报告
- 支持多维度统计分析

### 1.3 目标用户
| 用户角色 | 职责说明 |
|---------|---------|
| 系统管理员 | 配置系统基础数据：系统类型、模块库、阶梯参数、加权系数等 |
| 评估用户 | 创建评估项目、执行工作量评估、查看统计结果、导出评估报告 |

## 二、核心业务流程

### 2.1 整体业务流程
```
管理员配置基础数据 → 用户创建评估项目 → 填写评估指标 → 系统计算工作量 → 查看统计结果 → 导出评估报告
```

### 2.2 用户操作流程
1. **创建评估项目**：填写项目基本信息（项目名称、客户名称、源系统、目标系统等）
2. **选择迁移模块**：从模块库选择需要迁移的模块，可调整模块加权系数
3. **填写评估指标**：录入数据量、用户数、报表数、客开情况等指标
4. **系统自动计算**：根据评估模型自动计算工作量
5. **查看统计结果**：多维度查看工作量统计
6. **导出评估报告**：导出详细的工作量评估表

## 三、功能需求详细说明

### 3.1 系统管理模块（管理员）

#### 3.1.1 系统管理
**业务场景**：管理员需要维护ERP系统类型信息，为评估项目提供系统选择。

**功能描述**：
- 管理升迁涉及的ERP系统类型（如：SAP、Oracle、用友、金蝶等）
- 系统分为源系统和目标系统两类
- 支持一对一的升迁场景（不支持系统组合）

**功能要点**：
- 新增系统类型（系统名称、系统类型、描述）
- 编辑系统类型信息
- 启用/禁用系统类型
- 删除未被项目使用的系统类型
- 查看系统类型列表

#### 3.1.2 模块库管理
**业务场景**：管理员需要维护各ERP系统的标准模块及其基础工作量配置。

**功能描述**：
- 管理各系统的标准模块清单（如：财务管理、供应链管理、生产管理等）
- 配置每个模块的基础人天和默认加权系数
- 同一模块在不同系统中可配置不同的基础人天

**功能要点**：
- 新增模块（模块名称、所属系统、基础人天、默认加权系数、描述）
- 编辑模块信息
- 配置模块基础人天（单位：人天）
- 配置模块默认加权系数
- 启用/禁用模块
- 删除未被项目使用的模块
- 支持模块分类/分组管理
- 查看模块列表（支持按系统筛选）

**示例数据**：
| 模块名称 | 所属系统 | 基础人天 | 默认加权系数 |
|---------|---------|---------|------------|
| 财务管理 | SAP | 15 | 1.2 |
| 财务管理 | 用友 | 12 | 1.0 |
| 供应链管理 | SAP | 20 | 1.5 |
| 生产管理 | SAP | 25 | 1.8 |

#### 3.1.10 模块库分页能力（2026-06-09 追加）

**业务场景**：随着项目增多，模块库累计模块数会增长到几百甚至上千条（每个 ERP 系统几十到上百模块 × N 个系统）。当前前端一次性加载全表数据渲染会有两个问题：① 浏览器渲染卡顿；② 后端单次响应体过大、网络传输慢。需要给模块库管理页面加分页能力。

**功能描述**：
- 模块库列表支持分页查询，默认每页 10 条
- 支持的 pageSize 选项：10 / 20 / 50 / 100
- 翻页时保留当前的搜索/筛选条件（moduleName、systemId、category、status）
- 总记录数与表格分离展示："共 X 条"
- 支持页码跳转、首页/上一页/下一页/末页按钮
- 排序：按 create_time DESC（创建时间倒序，最新创建的在前）；同一批插入的多条记录顺序由 MySQL 自然顺序决定，可接受轻微跳序

**功能要点**：
- 后端 `/api/modules` 接口必须支持 `pageNum` + `pageSize` 参数，返回 `PageResult<ModuleVO>` 结构
- 筛选条件（moduleName 模糊、systemId、category、status）必须与分页参数组合生效
- pageSize 必须在 [1, 200] 范围内，超出按 200 处理
- pageNum 必须 ≥ 1，超出按 1 处理
- 搜索/筛选条件变化时，自动重置 pageNum=1

**验收标准**：
- 数据量为 0 时，表格显示空状态、分页组件显示 "共 0 条" 且不可翻页
- 数据量 < pageSize 时，所有记录显示在第 1 页，分页器不显示翻页按钮
- 数据量 > pageSize 时，分页器正确显示总页数和当前页码
- 翻页后筛选条件不丢失
- 改变 pageSize 后立即重新查询
- 跳转到不存在的页码（pageNum > 总页数）时返回最后一页数据，不报错

**示例**：
| 场景 | 期望结果 |
|------|----------|
| 0 条数据 | 表格空 + 分页器"共 0 条" |
| 5 条数据，pageSize=10 | 全部在第 1 页，无翻页按钮 |
| 25 条数据，pageSize=10 | 共 3 页，第 1 页显示前 10 条 |
| 改 pageSize=20 | 立即重新查询，表格显示前 20 条 |
| pageNum=99（超过总页数）| 返回第 3 页（最后一页）数据 |

#### 3.1.3 数据量阶梯配置
**业务场景**：管理员需要定义数据量分段标准及对应的工作量系数。

**功能描述**：
- 配置数据量阶梯范围（如：10万以内、10万-100万等）
- 为每个阶梯设置对应的工作量系数
- 阶梯数量和范围支持灵活调整

**功能要点**：
- 新增阶梯（阶梯名称、数据量范围、对应系数）
- 编辑阶梯信息
- 调整阶梯顺序
- 删除阶梯
- 查看阶梯列表

**默认阶梯示例**：
| 阶梯名称 | 数据量范围 | 对应系数 |
|---------|-----------|---------|
| 小型 | 0 - 10万 | 0.8 |
| 中型 | 10万 - 100万 | 1.0 |
| 大型 | 100万 - 1000万 | 1.5 |
| 超大型 | 1000万以上 | 2.0 |

#### 3.1.4 用户数阶梯配置
**业务场景**：管理员需要定义用户数分段标准及对应的工作量系数。

**功能描述**：
- 配置用户数阶梯范围（如：100用户以内、100-500用户等）
- 为每个阶梯设置对应的工作量系数
- 阶梯数量和范围支持灵活调整

**功能要点**：
- 新增阶梯（阶梯名称、用户数范围、对应系数）
- 编辑阶梯信息
- 调整阶梯顺序
- 删除阶梯
- 查看阶梯列表

**默认阶梯示例**：
| 阶梯名称 | 用户数范围 | 对应系数 |
|---------|-----------|---------|
| 小规模 | 0 - 100 | 1.0 |
| 中规模 | 100 - 500 | 1.2 |
| 大规模 | 500 - 1000 | 1.5 |
| 超大规模 | 1000以上 | 2.0 |

#### 3.1.5 报表系数配置
**业务场景**：管理员需要配置报表迁移的单位工作量系数。

**功能描述**：
- 设置报表迁移的单位工作量（人天/个）
- 系统根据报表数量自动计算报表迁移工作量

**功能要点**：
- 设置报表系数（单位：人天/个）

#### 3.1.6 用户管理
**业务场景**：管理员需要管理系统的用户账号。

**功能描述**：
- 创建用户账号
- 分配用户角色（管理员/普通用户）
- 管理用户状态

**功能要点**：
- 新增用户（用户名、密码、姓名、角色）
- 编辑用户信息
- 重置用户密码
- 启用/禁用用户
- 删除用户
- 查看用户列表

### 3.2 项目管理模块（用户）

#### 3.2.1 创建评估项目
**业务场景**：用户开始一个新的ERP升迁评估工作，需要创建评估项目。

**功能描述**：
- 创建新的评估项目
- 填写项目基本信息
- 选择源系统和目标系统

**项目基本信息**：
- 项目名称（必填）
- 客户名称（必填）
- 源系统（必填，从系统库选择）
- 目标系统（必填，从系统库选择）
- 项目负责人
- 联系方式
- 项目描述
- 评估日期（默认当前日期）

**业务规则**：
- 源系统和目标系统不能相同
- 项目名称不能重复
- 创建后项目状态为"草稿"

#### 3.2.2 项目列表管理
**业务场景**：用户需要查看和管理所有评估项目。

**功能描述**：
- 查看项目列表
- 搜索和筛选项目
- 管理项目状态

**列表展示信息**：
- 项目名称
- 客户名称
- 源系统 → 目标系统
- 项目负责人
- 评估日期
- 项目状态
- 操作按钮

**查询筛选功能**：
- 按项目名称搜索
- 按客户名称搜索
- 按项目状态筛选
- 按评估日期范围筛选

**操作功能**：
- 查看项目详情
- 编辑项目基本信息
- 删除项目（仅草稿状态可删除）
- 复制项目（创建副本，方便快速创建类似项目）

#### 3.2.3 项目状态管理
**业务场景**：跟踪评估项目的进展状态。

**项目状态定义**：
| 状态 | 说明 |
|------|------|
| 草稿 | 项目创建，未开始评估 |
| 进行中 | 正在评估中，已录入部分指标 |
| 已完成 | 评估完成，报告已生成 |
| 已归档 | 项目归档，只读状态 |

**状态流转规则**：
```
草稿 → 进行中 → 已完成 → 已归档
```
- 进入评估页面时，状态自动变为"进行中"
- 完成评估并生成报告后，状态变为"已完成"
- 项目归档后，状态变为"已归档"，只读不可修改

#### 3.2.4 项目状态历史（审计追溯）
**业务场景**：项目状态在草稿→进行中→已完成→已归档的流转过程中，需要记录每一次状态变更的时间、操作人、变更前后状态，供管理员审计、问题排查、数据分析使用。

**业务价值**：
- 审计合规：谁在什么时间把项目从 X 状态改到 Y 状态，可追溯
- 效率分析：评估项目从创建到完成的平均周期、卡在哪个阶段最久
- 问题排查：项目状态异常时，定位是哪一次流转出错
- 客户答疑：项目归档后客户问"什么时候完成的"，可立即查

**数据范围**：
- 每次 `project.status` 变更**自动记录**一行历史（不依赖业务代码主动写）
- 字段：
  - 历史记录ID
  - 项目ID
  - 变更前状态（from_status）：流转前的 project.status（首次创建时为空/DRAFT）
  - 变更后状态（to_status）：流转后的 project.status
  - 触发事件（event）：EVAL_START（进入评估）/ EVAL_COMPLETE（完成评估）/ ARCHIVE（归档）/ MANUAL_EDIT（手动编辑）
  - 操作人（operator）：触发流转的用户名（来自 JWT 上下文）
  - 备注（reason）：可选的业务备注（归档时可填"客户验收完成"等）
  - 变更时间（change_time）：状态变更时刻（精确到秒）

**权限要求**：
- 所有登录用户可**查看**自己创建项目的状态历史
- ADMIN 角色可**查看所有**项目的状态历史
- 仅 ADMIN 角色可**人工补录**（manual edit）状态历史
- 系统自动流转产生的历史**禁止手动编辑/删除**（`manual_edit=false` 为系统产生）

**接口需求**：
- `GET /api/projects/{projectId}/status-history`：分页查询某个项目的状态历史
  - 请求参数：`pageNum`、`pageSize`、`operator`（按操作人过滤，可选）
  - 响应：按 `change_time DESC` 排序，每行含 from_status、to_status、event、operator、reason、change_time
- `POST /api/projects/{projectId}/status-history`（仅 ADMIN）：人工补录历史记录
  - 用途：状态机上线前的老项目初始状态补录
  - 请求体：from_status / to_status / event / reason / change_time
  - 限制：`event=MANUAL_EDIT` 且 `manual_edit=true`

**业务规则**：
- 项目创建时**自动写入第一条**历史（from=DRAFT→to=DRAFT, event=CREATE）
- 状态机每次 `transition(projectId, event)` **自动追加一行**（同一事务内）
- 状态历史**永不删除**（只允许新增查询）
- 状态历史的 `from_status` 和 `to_status` 必须不同（无意义的同状态流转不写）
- 同一事务内多次流转只产生一行（避免短时多次点击产生噪音）

**非功能性需求**：
- 历史表数据量预估：每项目 3-5 条，100 个项目 ≈ 500 条
- 查询响应 < 200ms（10 万条数据下）
- 历史写入**不能阻塞**主业务（异步队列/同步写但加索引）

### 3.3 工作量评估模块

#### 3.3.1 评估流程设计
**业务场景**：用户按照步骤完成工作量评估。

**评估步骤**：

**步骤一：系统确认**
- 显示项目已配置的源系统和目标系统
- 如需修改，需返回项目基本信息编辑

**步骤二：选择迁移模块**
**功能描述**：
- 从模块库加载该源系统的所有可用模块
- 用户选择需要迁移的模块（多选）
- 显示每个模块的基础人天和默认加权系数
- 支持调整模块的加权系数（项目级别调整，不影响全局配置）

**界面展示要素**：
- 模块列表（复选框选择）
- 模块名称
- 基础人天
- 默认加权系数
- 调整系数输入框
- 最终系数（显示调整后的系数）

**业务规则**：
- 至少选择一个模块
- 调整系数可以大于或小于默认系数
- 如果未调整，使用默认系数

**步骤三：填写评估指标**
**业务描述**：录入各项评估指标数据。

##### 指标1：模块相关信息
- 显示已选模块列表
- 显示每个模块的基础人天和最终系数

##### 指标2：数据库表数量
- 录入需要迁移的数据库表总数量
- 单位：个

##### 指标3：数据量
- 录入数据总量
- 单位：万条
- 系统根据数据量自动匹配数据量阶梯系数
- 显示匹配的阶梯名称

##### 指标4：用户数量
- 录入系统用户总数
- 系统根据用户数自动匹配用户数阶梯系数
- 显示匹配的阶梯名称

##### 指标5：报表数量
- 录入需要迁移的报表总数
- 单位：个

##### 指标6：客开情况
- 选择是否有客开：是/否
- 如果选择"是"：
  - 录入客开模块数量
  - 录入客开评估人天数（手动输入）

##### 指标7：数据清洗需求
- 录入数据清洗需求描述（文本）
- 选择数据清洗复杂度（简单/中等/复杂）
- 注意：数据清洗需求仅作为参考信息，不计入工作量计算

**步骤四：计算工作量**
- 系统根据评估模型自动计算工作量
- 显示计算结果和详细明细

#### 3.3.2 工作量评估模型

**模型概述**：
工作量评估模型包含三个维度：核心迁移工作量、报表工作量、客开工作量。

**总工作量计算公式**：
```
总工作量（人天） = 核心迁移工作量 + 报表工作量 + 客开工作量
```

##### 1. 核心迁移工作量

**计算公式**：
```
核心迁移工作量 = Σ(各模块基础人天 × 模块加权系数 × 数据量阶梯系数 × 用户数阶梯系数)
```

**计算逻辑**：
1. 遍历所有选中的模块
2. 对每个模块计算工作量：
   ```
   模块工作量 = 模块基础人天 × 最终加权系数 × 数据量阶梯系数 × 用户数阶梯系数
   ```
   - **模块基础人天**：从模块库配置获取
   - **最终加权系数**：默认系数或用户调整后的系数
   - **数据量阶梯系数**：根据录入的数据量，系统自动匹配阶梯系数
   - **用户数阶梯系数**：根据录入的用户数，系统自动匹配阶梯系数
3. 累加所有模块的工作量

**计算示例**：
假设：
- 选择了财务管理模块：基础人天15天，调整系数1.3
- 选择了供应链管理模块：基础人天20天，使用默认系数1.5
- 数据量：500万条（阶梯系数1.5）
- 用户数：600人（阶梯系数1.5）

计算：
```
财务管理模块工作量 = 15 × 1.3 × 1.5 × 1.5 = 43.875人天
供应链管理模块工作量 = 20 × 1.5 × 1.5 × 1.5 = 67.5人天
核心迁移工作量 = 43.875 + 67.5 = 111.375人天
```

##### 2. 报表工作量

**计算公式**：
```
报表工作量 = 报表数量 × 报表系数
```

**计算逻辑**：
- 报表数量：用户录入
- 报表系数：从系统配置获取（单位：人天/个）

**计算示例**：
```
报表数量：50个
报表系数：0.5人天/个
报表工作量 = 50 × 0.5 = 25人天
```

##### 3. 客开工作量

**计算公式**：
```
客开工作量 = 客开评估人天数（手动输入）
```

**计算逻辑**：
- 如果有客开，用户手动录入客开人天数
- 如果没有客开，客开工作量为0

**计算示例**：
```
客开工作量 = 20人天（手动输入）
```

##### 4. 总工作量汇总

**计算公式**：
```
总工作量 = 核心迁移工作量 + 报表工作量 + 客开工作量
```

**计算示例**：
```
总工作量 = 111.375 + 25 + 20 = 156.375人天
```

**结果展示**：
- 显示总工作量（保留2位小数）
- 显示各部分工作量明细
- 显示各模块工作量明细

#### 3.3.3 工作量明细展示
**业务场景**：用户需要查看工作量计算的详细过程。

**展示内容**：
1. **核心迁移工作量明细表**
   - 模块名称
   - 基础人天
   - 加权系数
   - 数据量阶梯系数
   - 用户数阶梯系数
   - 模块工作量
   - 小计

2. **工作量汇总表**
   - 核心迁移工作量
   - 报表工作量
   - 客开工作量
   - 总工作量

### 3.4 工作量统计展示模块

#### 3.4.1 多维度统计分析
**业务场景**：用户需要从多个维度查看工作量分布情况。

**统计维度**：

##### 1. 按模块统计
- 各模块工作量占比（饼图）
- 各模块工作量排名（柱状图）
- 显示每个模块的工作量和占比

##### 2. 按工作量类型统计
- 核心迁移工作量 vs 报表工作量 vs 客开工作量的占比（饼图）
- 各类型工作量明细（表格）

##### 3. 按复杂度统计
- 显示数据量阶梯信息
- 显示用户数阶梯信息
- 高复杂度模块提示

##### 4. 总体汇总
- 总工作量（人天）
- 预估工期建议（根据工作量换算）
- 关键指标概览（模块数、数据量、用户数、报表数）

#### 3.4.2 可视化展示
**业务场景**：通过图表直观展示工作量分布。

**图表类型**：
- **饼图**：工作量类型分布、模块工作量占比
- **柱状图**：模块工作量对比
- **雷达图**：多维度评估指标（数据量、用户数、模块数、报表数、客开等）
- **仪表盘**：总工作量展示
- **明细表格**：工作量计算过程

**交互功能**：
- 图表支持悬停显示详细数据
- 表格支持排序
- 支持展开查看计算过程

#### 3.4.2 全局统计仪表盘（管理员）

**业务场景**：管理员需要从**全公司**视角看已完成评估项目的工作量分布、风险集中度、TOP 客户项目，以便做资源调度和报价决策。

**与单项目统计的区别**：
- 单项目统计（§3.4.1）：在单个项目评估完成后展示，**仅展示该项目**的图表
- 全局统计（§3.4.2）：在"统计可视化"菜单下独立页面，**跨项目聚合**所有已完成评估的数据

**统计维度**：

##### 1. 全局概览
- 已评估项目数
- 总工作量（人天）
- 平均工作量（人天/项目）
- 总工时（人月，按 22 人天/人月换算）
- 已选模块总量（去重）

##### 2. 按模块聚合
- 各模块在所有项目中被选中的累计工作量（饼图 + Top 10 柱状图）
- 每个模块被多少项目选中
- 占比 = 该模块累计工作量 / 全公司总工作量

##### 3. 按工作量类型聚合
- 全公司层面：核心迁移 vs 报表 vs 客开 的累计工作量占比
- 包含每种类型在多少项目中出现

##### 4. 按复杂度聚合
- 数据量阶梯分布（每个阶梯多少项目、累计工作量）
- 用户数阶梯分布（同上）
- 高复杂度模块清单（系数 ≥ 1.5 的模块，在多少项目中被选中）

##### 5. 排行榜
- 按工作量排序的 Top N 项目（默认 10，可选 1-50）
- 按用户数排序的 Top N 项目
- 按数据量排序的 Top N 项目

**筛选条件**：
- 评估日期范围（dateFrom / dateTo）
- 源系统类型（单选下拉）
- 目标系统类型（单选下拉）
- 项目状态（默认仅 `COMPLETED`，可放开查看其他状态）

**权限要求**：
- 全局统计仅 `ADMIN` 角色可访问
- 普通用户访问时返回 403 + 业务码 11001

**交互要求**：
- 顶部筛选条 + 4 个聚合卡片 + 排行榜
- 各图表支持悬停 tooltip 显示明细
- 点击模块/项目名可跳转到对应列表页（带预选过滤）
- 数据为空时显示"暂无已完成评估数据"占位，不显示空白图表

---

### 3.5 评估报告输出模块

#### 3.5.1 报告内容结构
**业务场景**：生成完整的评估报告供用户下载。

**报告包含以下章节**：

##### 1. 项目基本信息
- 项目名称
- 客户名称
- 源系统 → 目标系统
- 项目负责人
- 联系方式
- 评估日期
- 项目状态

##### 2. 评估指标汇总
- 选择模块数量及清单
- 数据库表数量
- 数据量及阶梯
- 用户数量及阶梯
- 报表数量
- 客开情况（客开模块数、客开人天）
- 数据清洗需求

##### 3. 工作量评估明细
**核心迁移工作量明细表**：
| 模块名称 | 基础人天 | 加权系数 | 数据量系数 | 用户数系数 | 模块工作量 |
|---------|---------|---------|-----------|-----------|-----------|
| 财务管理 | 15 | 1.3 | 1.5 | 1.5 | 43.88 |
| 供应链管理 | 20 | 1.5 | 1.5 | 1.5 | 67.50 |
| ... | ... | ... | ... | ... | ... |
| **小计** | - | - | - | - | **111.38** |

**工作量汇总表**：
| 工作量类型 | 工作量（人天） | 占比 |
|-----------|--------------|------|
| 核心迁移工作量 | 111.38 | 71.3% |
| 报表工作量 | 25.00 | 16.0% |
| 客开工作量 | 20.00 | 12.7% |
| **总工作量** | **156.38** | **100%** |

##### 4. 可视化图表
- 工作量类型分布饼图
- 模块工作量对比柱状图
- 多维度评估雷达图

##### 5. 风险提示与建议
- 高复杂度模块提示（系数较高的模块）
- 数据量风险提示（大型、超大型数据量）
- 用户数风险提示（大规模用户）
- 客开风险提示
- 数据迁移建议

#### 3.5.2 导出格式
**业务场景**：用户需要不同格式的报告用于不同用途。

**支持格式**：
- **Excel格式**（.xlsx）
  - 详细数据表
  - 支持后续编辑
  - 包含所有明细数据

- **PDF格式**（.pdf）
  - 正式报告文档
  - 排版美观，适合打印
  - 包含图表

- **Word格式**（.docx）
  - 可编辑文档
  - 适合二次修改
  - 包含图表

#### 3.5.3 导出功能
**业务场景**：用户导出评估报告。

**操作流程**：
1. 在评估结果页面点击"导出报告"
2. 选择导出格式（Excel/PDF/Word）
3. 选择导出内容范围（全部章节/部分章节）
4. 点击确认导出
5. 系统生成文件并自动下载

**文件命名规则**：
```
{客户名称}_{项目名称}_工作量评估报告_{日期}.{格式}

示例：
XX公司_ERP迁移项目_工作量评估报告_20260319.xlsx
```

## 四、非功能性需求

### 4.1 用户界面需求
- **响应式设计**：支持PC端主流浏览器（Chrome、Firefox、Edge、Safari）
- **界面友好**：操作流程清晰，引导明确
- **数据展示**：表格支持排序、筛选、分页
- **表单验证**：实时输入验证，友好的错误提示
- **帮助提示**：关键操作提供帮助提示

### 4.2 性能需求
- **页面加载**：页面加载时间 < 2秒
- **工作量计算**：评估计算响应时间 < 1秒
- **报告生成**：PDF/Excel生成时间 < 5秒
- **并发支持**：支持50个用户同时在线操作

### 4.3 安全性需求
- **用户认证**：登录认证、会话管理
- **权限控制**：基于角色的权限控制（管理员/普通用户）
- **数据安全**：密码加密存储、敏感数据保护
- **操作审计**：记录用户关键操作日志
- **数据隔离**：不同用户的数据互相隔离

### 4.4 数据完整性需求
- **数据校验**：关键数据输入验证
- **业务规则**：业务规则校验（如源系统≠目标系统）
- **数据备份**：定期数据备份

### 4.5 易用性需求
- **操作简便**：评估流程分步骤引导
- **实时反馈**：操作后及时反馈结果
- **错误处理**：友好的错误提示和解决建议
- **在线帮助**：提供操作指南和帮助文档

### 4.6 可维护性需求
- **系统配置**：关键参数可配置（如阶梯、系数）
- **日志记录**：完善的系统运行日志
- **异常处理**：系统异常捕获和处理

## 五、业务规则

### 5.1 系统规则
1. 源系统和目标系统不能相同
2. 项目名称不能重复
3. 项目至少选择一个模块
4. 工作量计算保留2位小数

### 5.2 状态规则
1. 项目状态流转：草稿 → 进行中 → 已完成 → 已归档
2. 只有草稿状态的项目可以删除
3. 已归档的项目为只读状态，不可修改

### 5.3 计算规则
1. 未调整系数的模块使用默认系数
2. 数据量和用户数根据阶梯自动匹配系数
3. 无客开时，客开工作量为0
4. 总工作量 = 核心迁移工作量 + 报表工作量 + 客开工作量

### 5.4 配置规则
1. 被项目引用的系统类型、模块不能删除
2. 阶梯配置至少保留一个
3. 系数可以为小数，支持小于1或大于1

## 六、约束与假设

### 6.1 业务约束
- 系统仅支持一对一的ERP系统升迁评估，不支持多系统组合升迁
- 工作量评估模型为预设模型，用户在项目级别可调整模块系数
- 数据清洗需求不计入工作量计算，仅作为参考信息

### 6.2 技术约束
- 系统为Web应用，需支持主流浏览器
- 系统需支持数据导出功能

### 6.3 假设条件
- 假设用户提供的评估数据准确
- 假设工作量评估模型合理有效
- 假设模块库配置完善

## 七、术语表

| 术语 | 说明 |
|------|------|
| ERP | 企业资源计划系统（Enterprise Resource Planning） |
| 客开 | 客户定制开发，指在标准ERP系统基础上为客户定制的功能 |
| 人天 | 工作量单位，表示一个人工作一天的工作量 |
| 加权系数 | 用于调整工作量的系数，反映模块的复杂度 |
| 阶梯 | 数据量或用户数的分段范围，不同阶梯对应不同的工作量系数 |
| 异构系统 | 不同技术架构、不同厂商的系统 |
| 升迁 | 系统迁移，指从旧系统迁移到新系统 |
| 源系统 | 迁移前的旧系统 |
| 目标系统 | 迁移后的新系统 |

## 八、附录

### 8.1 参考文档
- 无

---

**文档版本**：V1.0
**创建日期**：2026-03-19
**最后更新**：2026-03-19
**文档状态**：待评审