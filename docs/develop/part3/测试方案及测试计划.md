# 第三阶段测试方案及测试计划

## 文档信息

| 项目名称 | 异构系统升迁工作量评估系统 (MigraMetric) |
|---------|----------------------------------------|
| 所属阶段 | 第三阶段：项目管理模块 |
| 文档版本 | V1.0 |
| 创建日期 | 2026-03-23 |
| 文档作者 | 开发团队 |

---

## 一、测试概述

### 1.1 测试范围

本测试计划针对第三阶段完成的 4 个模块：

| 模块 | 功能 | 后端 | 前端 |
|------|------|------|------|
| 3.1 项目列表 | 列表查询、分页、筛选、状态展示 | ✅ | ✅ |
| 3.2 创建项目 | 项目基本信息录入、数据校验 | ✅ | ✅ |
| 3.3 项目详情 | 详情查看、评估概况展示 | ✅ | ✅ |
| 3.4 项目操作 | 编辑、复制、删除（仅草稿）、归档（仅已完成） | ✅ | ✅ |

### 1.2 测试目标

1. **功能完整性**：所有 CRUD 操作正常工作
2. **业务逻辑正确性**：项目状态流转、数据校验、状态权限控制等业务逻辑正确
3. **前后端集成**：API 接口与前端页面正常交互
4. **代码质量**：通过 ESLint、TypeScript 检查

---

## 二、后端测试

### 2.1 Service 层测试（已覆盖）

| 模块 | 测试类 | 测试数量 | 覆盖内容 |
|------|--------|---------|---------|
| Project | ProjectServiceTest | 34 | CRUD、状态流转、业务校验 |

**后端 Service 测试总计：34 tests** ✅

#### 2.1.1 QueryPageTests（4 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| QPT-001 | 分页查询返回正确结果 | records.size=1, total=1 |
| QPT-002 | 查询结果包含系统名称 | 源/目标系统名称解析 |
| QPT-003 | 状态文本正确设置 | "草稿"/"进行中"等文本映射 |
| QPT-004 | 空结果返回空列表 | records=[], total=0 |

#### 2.1.2 GetByIdTests（2 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| GBI-001 | ID查询返回项目详情 | projectName/sourceSystemName匹配 |
| GBI-002 | ID不存在抛出异常 | BusinessException |

#### 2.1.3 GetProjectDetailTests（3 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| GPD-001 | 包含评估概况的项目详情 | workload值正确、selectedModuleCount |
| GPD-002 | 无评估返回默认值 | hasEvaluation=false, totalWorkload=null |
| GPD-003 | 项目不存在抛出异常 | BusinessException |

#### 2.1.4 CreateTests（6 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| CRT-001 | 成功创建项目 | 返回新项目ID |
| CRT-002 | 源系统和目标系统相同失败 | BusinessException + 错误消息 |
| CRT-003 | 源系统不存在失败 | BusinessException + 错误消息 |
| CRT-004 | 目标系统不存在失败 | BusinessException + 错误消息 |
| CRT-005 | 源系统已禁用失败 | BusinessException + 错误消息 |
| CRT-006 | 目标系统已禁用失败 | BusinessException + 错误消息 |

#### 2.1.5 UpdateTests（5 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| UPT-001 | 成功更新项目 | 无异常 |
| UPT-002 | 项目不存在抛出异常 | BusinessException + 错误消息 |
| UPT-003 | 已归档项目不可编辑 | BusinessException + 错误消息 |
| UPT-004 | 更新时源目标和目标系统相同失败 | BusinessException + 错误消息 |
| UPT-005 | 更新时校验目标系统 | 无异常，正常更新 |

#### 2.1.6 CopyTests（4 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| CPY-001 | 成功复制项目 | 返回新项目ID |
| CPY-002 | 项目不存在抛出异常 | BusinessException + 错误消息 |
| CPY-003 | 复制后名称添加_副本后缀 | 项目名称格式验证 |
| CPY-004 | 复制后状态为草稿 | status=DRAFT |

#### 2.1.7 DeleteTests（5 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| DEL-001 | 成功删除草稿项目 | 无异常 |
| DEL-002 | 项目不存在抛出异常 | BusinessException + 错误消息 |
| DEL-003 | 进行中状态不可删除 | BusinessException + 错误消息 |
| DEL-004 | 已完成状态不可删除 | BusinessException + 错误消息 |
| DEL-005 | 已归档状态不可删除 | BusinessException + 错误消息 |

#### 2.1.8 ArchiveTests（5 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| ARC-001 | 成功归档已完成项目 | 无异常 |
| ARC-002 | 项目不存在抛出异常 | BusinessException + 错误消息 |
| ARC-003 | 进行中状态不可归档 | BusinessException + 错误消息 |
| ARC-004 | 草稿状态不可归档 | BusinessException + 错误消息 |
| ARC-005 | 已归档不可再次归档 | BusinessException + 错误消息 |

### 2.2 Controller 层测试（待补充）

| 模块 | 测试类 | 计划测试数 | 覆盖内容 |
|------|--------|-----------|---------|
| Project | ProjectControllerTest | 10 | REST API 端点测试 |

**待补充测试用例：**

```java
// ProjectControllerTest.java
@SpringBootTest
@AutoConfigureMockMvc
class ProjectControllerTest {

    @Test
    @DisplayName("分页查询应返回正确结果")
    void pageQueryShouldReturnCorrectResult() {
        // GET /api/projects?pageNum=1&pageSize=10
    }

    @Test
    @DisplayName("创建项目应返回201")
    void createShouldReturn201() {
        // POST /api/projects
    }

    @Test
    @DisplayName("获取项目详情应返回项目信息")
    void getDetailShouldReturnProjectInfo() {
        // GET /api/projects/{id}/detail
    }

    @Test
    @DisplayName("更新项目应返回200")
    void updateShouldReturn200() {
        // PUT /api/projects/{id}
    }

    @Test
    @DisplayName("复制项目应返回新项目ID")
    void copyShouldReturnNewId() {
        // POST /api/projects/{id}/copy
    }

    @Test
    @DisplayName("删除项目应返回200")
    void deleteShouldReturn200() {
        // DELETE /api/projects/{id}
    }

    @Test
    @DisplayName("归档项目应返回200")
    void archiveShouldReturn200() {
        // POST /api/projects/{id}/archive
    }

    @Test
    @DisplayName("删除非草稿项目应返回400")
    void deleteNonDraftShouldReturn400() {
        // DELETE /api/projects/{id} (非草稿状态)
    }

    @Test
    @DisplayName("归档非完成项目应返回400")
    void archiveNonCompletedShouldReturn400() {
        // POST /api/projects/{id}/archive (非完成状态)
    }

    @Test
    @DisplayName("源目标相同时应返回400")
    void sourceEqualsTargetShouldReturn400() {
        // POST /api/projects (源系统ID=目标系统ID)
    }
}
```

---

## 三、前端测试

### 3.1 API 层测试（已覆盖）

| 模块 | 测试文件 | 测试数量 | 覆盖内容 |
|------|---------|---------|---------|
| Project | api/project/projects.test.ts | 13 | CRUD API 响应处理 |

**前端 API 测试总计：13 tests** ✅

#### 3.1.1 createProject（2 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-CRT-001 | 应成功创建项目并返回ID | POST /api/projects, 返回ID |
| API-CRT-002 | 应支持完整的创建参数 | 所有字段正确传递 |

#### 3.1.2 queryProjectPage（3 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-QRY-001 | 应返回项目分页列表 | records数组、total总数 |
| API-QRY-002 | 应正确传递查询参数 | params对象包含所有查询条件 |
| API-QRY-003 | 应支持分页参数 | pageNum/pageSize正确 |

#### 3.1.3 getProjectById（1 test）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-GBI-001 | 应根据ID获取项目详情 | GET /api/projects/{id} |

#### 3.1.4 getProjectDetail（2 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-GPD-001 | 应获取包含评估概况的项目详情 | hasEvaluation=true, workload值 |
| API-GPD-002 | 应正确处理无评估记录的项目详情 | hasEvaluation=false, null值 |

#### 3.1.5 updateProject（2 tests）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-UPT-001 | 应成功更新项目 | PUT /api/projects/{id} |
| API-UPT-002 | 应支持完整更新参数 | 所有字段正确传递 |

#### 3.1.6 copyProject（1 test）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-CPY-001 | 应成功复制项目并返回新项目ID | POST /api/projects/{id}/copy |

#### 3.1.7 deleteProject（1 test）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-DEL-001 | 应成功删除项目 | DELETE /api/projects/{id} |

#### 3.1.8 archiveProject（1 test）

| 测试编号 | 测试描述 | 验证点 |
|---------|---------|--------|
| API-ARC-001 | 应成功归档项目 | POST /api/projects/{id}/archive |

### 3.2 组件测试（待补充）

| 组件 | 测试文件 | 计划测试数 | 覆盖内容 |
|------|---------|-----------|---------|
| ProjectList | ProjectListView.test.ts | 12 | 渲染、交互、状态管理 |
| ProjectCreate | ProjectCreateView.test.ts | 10 | 渲染、表单验证 |
| ProjectDetail | ProjectDetailView.test.ts | 10 | 渲染、操作按钮 |
| ProjectEdit | ProjectEditView.test.ts | 8 | 渲染、表单更新 |

---

## 四、测试执行结果

### 4.1 当前测试统计

```
后端测试总计: 169 tests
├── Phase 1 基础测试 (50 tests)
│   ├── ResultTest: 9 tests ✅
│   ├── PageResultTest: 8 tests ✅
│   ├── BusinessExceptionTest: 7 tests ✅
│   ├── GlobalExceptionHandlerTest: 13 tests ✅
│   ├── CorsIntegrationTest: 7 tests ✅
│   └── ApplicationStartupTest: 6 tests ✅
├── Phase 2 系统配置测试 (73 tests)
│   ├── SystemTypeServiceTest: 17 tests ✅
│   ├── ModuleServiceTest: 14 tests ✅
│   ├── DataVolumeLadderServiceTest: 15 tests ✅
│   ├── UserCountLadderServiceTest: 15 tests ✅
│   └── ReportConfigServiceTest: 12 tests ✅
└── Phase 3 项目管理测试 (46 tests)
    ├── ProjectServiceTest: 34 tests ✅
    └── SystemTypeControllerTest: 12 tests ✅

前端测试总计: 149 tests
├── Phase 1 基础测试 (82 tests)
│   ├── validate.test.ts: 22 tests ✅
│   ├── format.test.ts: 17 tests ✅
│   ├── user.test.ts: 12 tests ✅
│   ├── request.test.ts: ~10 tests ✅
│   └── 其他: 21 tests ✅
├── Phase 2 API 测试 (35 tests)
│   ├── types.test.ts: 10 tests ✅
│   ├── modules.test.ts: 10 tests ✅
│   ├── dataVolume.test.ts: 8 tests ✅
│   ├── userCount.test.ts: 8 tests ✅
│   └── reportConfig.test.ts: 5 tests ✅
└── Phase 3 API 测试 (13 tests)
    └── projects.test.ts: 13 tests ✅
```

### 4.2 测试覆盖率目标

| 类别 | 目标覆盖率 | 当前覆盖率 | 差距 |
|------|-----------|-----------|------|
| 后端 ProjectService 层 | 90% | ~85% | 5% |
| 后端 ProjectController 层 | 80% | 0% | 80% |
| 前端 API 层 | 90% | ~80% | 10% |
| 前端组件层 | 60% | 0% | 60% |

---

## 五、待补充测试

### 5.1 Controller 集成测试

建议添加 ProjectController 集成测试：

```java
// ProjectControllerTest.java
@SpringBootTest
@AutoConfigureMockMvc
class ProjectControllerTest {

    @Test
    @DisplayName("创建项目应返回201")
    void createShouldReturn201() {
        // POST /api/projects
    }

    @Test
    @DisplayName("源目标相同时应返回400")
    void sourceEqualsTargetShouldReturn400() {
        // POST /api/projects (源系统ID=目标系统ID)
    }

    @Test
    @DisplayName("删除非草稿项目应返回400")
    void deleteNonDraftShouldReturn400() {
        // DELETE /api/projects/{id} (非草稿状态)
    }

    @Test
    @DisplayName("归档非完成项目应返回400")
    void archiveNonCompletedShouldReturn400() {
        // POST /api/projects/{id}/archive (非完成状态)
    }
}
```

### 5.2 前端组件测试

建议添加以下组件测试：

| 组件 | 测试内容 |
|------|---------|
| ProjectListView | 列表渲染、分页、搜索、删除确认、复制/归档按钮显示 |
| ProjectCreateView | 表单验证、系统选择、日期选择、提交 |
| ProjectDetailView | 详情展示、状态标签、操作按钮 |
| ProjectEditView | 表单加载、数据更新、保存 |

---

## 六、集成测试建议

### 6.1 端到端测试场景

| 场景 | 测试步骤 | 预期结果 |
|------|---------|---------|
| 项目完整流程 | 创建 → 查看列表 → 查看详情 → 编辑 → 删除 | 全流程正常 |
| 项目复制流程 | 选择项目 → 复制 → 查看新项目 → 验证数据 | 复制成功，数据一致 |
| 项目归档流程 | 完成评估 → 归档项目 → 验证只读 | 归档成功，不可编辑 |

### 6.2 异常场景测试

| 场景 | 输入 | 预期结果 |
|------|------|---------|
| 创建重复项目 | 项目名称已存在 | 返回错误：项目名称已存在 |
| 删除进行中项目 | 删除IN_PROGRESS状态项目 | 返回错误：只有草稿状态的项目可以删除 |
| 归档草稿项目 | 归档DRAFT状态项目 | 返回错误：只有已完成的项目可以归档 |
| 编辑已归档项目 | 编辑ARCHIVED状态项目 | 返回错误：已归档的项目不可编辑 |

---

## 七、测试执行命令

### 7.1 后端测试

```bash
# 运行所有测试
cd migrametric-server
mvn clean test

# 运行项目服务测试
mvn test -Dtest=ProjectServiceTest

# 运行指定测试类
mvn test -Dtest=ProjectServiceTest\$CreateTests
mvn test -Dtest=ProjectServiceTest\$UpdateTests
mvn test -Dtest=ProjectServiceTest\$CopyTests
mvn test -Dtest=ProjectServiceTest\$DeleteTests
mvn test -Dtest=ProjectServiceTest\$ArchiveTests

# 生成覆盖率报告
mvn test jacoco:report
```

### 7.2 前端测试

```bash
# 运行所有测试
cd migrametric-web
npm run test:unit

# 运行项目相关测试
npm test -- --run src/api/project/projects.test.ts

# 类型检查
npm run type-check

# ESLint 检查
npm run lint

# 构建检查
npm run build
```

---

## 八、业务规则测试矩阵

### 8.1 项目状态机

```
DRAFT(草稿) → IN_PROGRESS(进行中) → COMPLETED(已完成) → ARCHIVED(已归档)
```

| 操作 \ 状态 | DRAFT | IN_PROGRESS | COMPLETED | ARCHIVED |
|------------|-------|-------------|-----------|----------|
| 编辑 | ✅ | ✅ | ✅ | ❌ |
| 删除 | ✅ | ❌ | ❌ | ❌ |
| 归档 | ❌ | ❌ | ✅ | ❌ |
| 复制 | ✅ | ✅ | ✅ | ✅ |

### 8.2 业务校验规则

| 规则编号 | 规则描述 | Create | Update | 测试覆盖 |
|---------|---------|--------|--------|---------|
| R001 | 源系统和目标系统不能相同 | ✅ | ✅ | ✅ |
| R002 | 源系统必须存在且启用 | ✅ | ✅ | ✅ |
| R003 | 目标系统必须存在且启用 | ✅ | ✅ | ✅ |
| R004 | 已归档项目不可编辑 | N/A | ✅ | ✅ |
| R005 | 只有草稿可删除 | N/A | N/A | ✅ |
| R006 | 只有已完成可归档 | N/A | N/A | ✅ |
| R007 | 项目不存在时抛出异常 | ✅ | ✅ | ✅ |

---

## 九、测试结论

### 9.1 第三阶段测试通过标准

| 标准项 | 状态 | 说明 |
|-------|------|------|
| ProjectService 层测试全部通过 | ✅ | 34 tests passed |
| 前端 API 测试全部通过 | ✅ | 13 tests passed |
| 代码编译通过 | ✅ | 无编译错误 |
| ESLint 检查通过 | ✅ | 0 errors (仅预存warnings) |
| TypeScript 检查通过 | ✅ | 无编译错误 |

### 9.2 测试遗留项

| 问题编号 | 问题描述 | 优先级 | 负责人 | 计划完成 |
|---------|---------|--------|-------|---------|
| ISSUE-301 | ProjectController 层集成测试缺失 | P2 | 开发团队 | 第四阶段 |
| ISSUE-302 | 前端组件测试缺失 | P2 | 开发团队 | 第七阶段 |
| ISSUE-303 | 前端 E2E 端到端测试 | P3 | 开发团队 | 第七阶段 |

---

## 十、附录

### 10.1 快速测试命令

```bash
# 后端测试
cd migrametric-server
mvn clean test                              # 清理并运行所有测试
mvn test -Dtest=ProjectServiceTest          # 运行项目服务测试
mvn test jacoco:report                     # 生成覆盖率报告

# 前端测试
cd migrametric-web
npm run test:unit                            # 运行所有单元测试
npm run type-check                           # TypeScript 类型检查
npm run lint                                 # ESLint 检查
npm run build                                # 生产构建
```

### 10.2 测试环境说明

**后端测试环境**：使用H2内存数据库，与MySQL兼容模式
**前端测试环境**：使用jsdom模拟浏览器环境
**集成测试**：使用@SpringBootTest进行真实环境测试

---

**文档结束**

---

## 版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|---------|------|
| V1.0 | 2026-03-23 | 初始版本 | 开发团队 |
